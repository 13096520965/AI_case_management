# 首页待办事项简洁样式

## 最终样式要求

未读消息样式：
- ✅ 左侧红点标注
- ✅ 彩色图标（根据类型）
- ❌ 不需要背景色
- ❌ 不需要 border-left

## 实现效果

### 视觉呈现

```
┌─────────────────────────────────────────┐
│ 待办事项  [5]                    [刷新]  │
├─────────────────────────────────────────┤
│ ● [⚠] 节点已逾期，请尽快处理            │  ← 红点 + 红色图标
│        案件编号: 2024-001               │
│        2小时前                          │
├─────────────────────────────────────────┤
│ ● [🕐] 节点即将到期                     │  ← 红点 + 橙色图标
│        案件编号: 2024-002               │
│        3小时前                          │
├─────────────────────────────────────────┤
│ ● [💰] 待支付费用提醒                   │  ← 红点 + 蓝色图标
│        案件编号: 2024-003               │
│        昨天                             │
└─────────────────────────────────────────┘
```

## 样式代码

### 列表项基础样式

```css
.todo-item {
  position: relative;
  display: flex;
  align-items: flex-start;
  padding: 12px 16px;
  cursor: pointer;
  transition: background-color 0.3s;
}

/* 悬停效果 - 统一的浅灰色背景 */
.todo-item:hover {
  background-color: #f5f7fa;
}
```

### 红点样式

```css
.item-dot {
  position: absolute;
  left: 8px;
  top: 18px;
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background-color: #f56c6c;  /* 红色 */
}
```

### 图标样式

```css
.item-icon {
  flex-shrink: 0;
  margin-right: 12px;
  margin-top: 2px;
}
```

## 模板结构

```html
<div 
  class="todo-item"
  @click="handleAlertClick(item)"
>
  <!-- 红点 - 所有未读消息都显示 -->
  <div class="item-dot" v-if="item.status === 'unread'"></div>
  
  <!-- 彩色图标 - 根据类型显示不同颜色 -->
  <div class="item-icon">
    <el-icon :size="18" :color="getNotificationColor(item)">
      <component :is="getNotificationIcon(item)" />
    </el-icon>
  </div>
  
  <!-- 内容 -->
  <div class="item-content">
    <div class="item-text">{{ item.content }}</div>
    <div class="item-desc" v-if="item.caseNumber">案件编号: {{ item.caseNumber }}</div>
    <div class="item-time">{{ formatTime(item.scheduledTime) }}</div>
  </div>
</div>
```

## 图标颜色映射

| 类型 | 图标 | 颜色 | 说明 |
|------|------|------|------|
| overdue | Warning | #F56C6C | 逾期 - 红色 |
| deadline | Clock | #E6A23C | 截止 - 橙色 |
| payment | Money | #409EFF | 支付 - 蓝色 |
| task | Document | #67C23A | 任务 - 绿色 |
| 其他 | Bell | #909399 | 默认 - 灰色 |

## 状态标识

### 未读状态
- 左侧红点（6px 圆形，红色 #f56c6c）
- 彩色图标（根据类型）
- 无背景色
- 无边框

### 悬停状态
- 浅灰色背景（#f5f7fa）
- 其他保持不变

### 点击后
- 标记为已读
- 从列表移除
- 未读数量 -1

## 移除的样式

以下样式已被移除：

```css
/* ❌ 不再使用 */
.todo-item.is-unread {
  background-color: #f0f9ff;
}

.todo-item.is-unread:hover {
  background-color: #e6f4ff;
}

.todo-item.high {
  border-left: 3px solid #f56c6c;
}

.todo-item.medium {
  border-left: 3px solid #e6a23c;
}

.todo-item.low {
  border-left: 3px solid #409eff;
}
```

## 设计原则

1. **简洁明了** - 只用红点和图标颜色标识
2. **视觉统一** - 所有消息使用相同的背景（白色）
3. **重点突出** - 红点醒目，易于识别未读状态
4. **类型清晰** - 图标颜色区分不同类型
5. **交互友好** - 悬停时有反馈，点击后立即更新

## 测试要点

1. ✅ 所有未读消息显示红点
2. ✅ 红点位置在左侧（left: 8px）
3. ✅ 图标颜色根据类型变化
4. ✅ 没有背景色区分（除了悬停）
5. ✅ 没有 border-left 样式
6. ✅ 悬停时显示浅灰色背景
7. ✅ 点击后红点消失（消息被移除）
